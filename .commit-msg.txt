fix: MCP row button matrix per user ruling + git classifier hardening

Button matrix (streamlined, user-approved direction):
- ONE switch per manageable row: ON enters gateway as full (native pinned,
  in-process registration), OFF stays gateway-owned but silent. The native
  toggle dual-brain is gone, so rows whose gateway entry was lost can always
  re-enter management (root cause of the missing 标准参数/工具参数 button).
- 工具参数 (full/context-lite) shows only while ON; one click, no popup.
- 测试/查更新 revert to stdio-only rows (wave-2 over-loosening reverted).
- Mode switching no longer pops confirm dialogs; host-side confirm gate kept
  as API protection; the genuinely risky hot-reload confirm stays.

Git classifier (SodaMem/dsh-plugin-sodamem shown as 非 DSH 插件):
- root cause: MCP-bridge package.json read failures were silently classified
  "unknown", indistinguishable from truly-not-dsh;
- now: one retry, a distinct 读取失败 badge with refresh hint, and cordis
  artifacts (cordis.patch.yml / agent.cordis.yml) count as a second identity
  signal even without the dsh field;
- regression: 5 classifier fixtures (dsh-field / cordis-artifact / bin /
  read-error / plain) in tests/regression.mjs; live re-probe classifies
  sodamem as dsh-plugin.

Suites: regression 39, server-modes 24, headless render — all green.
